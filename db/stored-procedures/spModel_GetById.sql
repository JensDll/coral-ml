SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO
CREATE OR ALTER PROCEDURE [dbo].[spModel_GetById]
  @id int
AS
BEGIN
  SET NOCOUNT ON;

  SELECT id, modelName, model
  FROM dbo.Model
  WHERE id = @id
END
GO